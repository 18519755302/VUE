//导入
import {
    install,
    Store
} from './store';

import {
    mapState,
    mapGetters,
    mapActions,
    mapMutations
} from './helper';

//导出
export default {
    install,
    Store,
    mapState,
    mapGetters,
    mapActions,
    mapMutations
}

export {
    install,
    Store,
    mapState,
    mapGetters,
    mapActions,
    mapMutations
}